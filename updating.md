# updating.md —— 本次改动维护说明：「角色空间（娱乐）」

> **合并说明**：这是本次改动的 agent.md 章节草稿，合并时把下面每个 `## … ` 小节原样并入主 `agent.md` 即可（若标题层级冲突，整体降一级）。文中文件名 / 行号以本次工作区为准。
>
> **入库提醒**：仓库 `.gitignore` 只忽略了 `agent.md`（"开发者 / Agent 维护文档，不进开源仓库"），`updating.md` 目前**未被忽略**——合并进 `agent.md` 之后，如果不想让它随开源仓库走，删掉它或把它加进 `.gitignore` 即可。

## 背景与红线：为什么角色扮演必须独立

设计讨论的结论（用户确认）：**语气 ≠ 身份**。

- 工作会话的能力契约来自系统提示里的工具目录、sandbox、approval、runtime context。把"融入角色"塞进工作会话（大段角色卡，或 `complete persona`）会**静默**侵蚀事实性、边界判断与工具调用风格，而且污染的是用户真正干活的那条历史。
- 所以本插件对工作会话**只保留语气注入**（`xiao-voice` section，语义不变），角色的"身份"落到 **DSH 的一等机制：agent preset**（`<dshHome>/.agent-presets/<id>/`）配合 `dsh-persona` 的 `complete: true`，并在**独立会话**里使用。
- 隔离不靠提示词约束，而靠**能力差异**：角色预设**不挂任何工具**，所以"角色扮演消解真实能力"在结构上不可能发生——代价是角色会话也不能干活，这正是娱乐模式应有的边界。

## 数据模型与向后兼容

`src/config.ts`（Host / Client 共享类型）新增两个字段：

| 字段 | 类型 | 默认 | 语义 |
| --- | --- | --- | --- |
| `roleplayEnabled` | boolean | **false** | 一键开关。开 = 生成 / 更新预设；关 = 移除预设。**受 `enabled`（主题总开关）门控**：`enabled=false` 时不安装并移除预设（生效条件 = `enabled !== false && roleplayEnabled === true`）。默认**关闭**：不打开就什么都不装，升级不会凭空多出一个 agent preset |
| `roleplayPersona` | string | `''` | 角色会话的完整 system prompt；空 = 用 Host 内置的英文「魈」卡 |

兼容规则（改这块时必须保持）：

- 旧配置 / 旧主题文件 / 旧导出 JSON 缺字段 → 走 `normalizeConfig` 的默认值（`roleplayEnabled === true` 才为真，**缺失即 false**；`roleplayPersona` 非字符串则取默认）。
- **"缺失即 false"是有意的**：上一版默认开启，只在设置页保存过的人才会把 `roleplayEnabled: true` 落进配置；从未保存过的人升级后会被判定为关闭，旧版本替他自动装上的预设会被 `syncRoleplayPreset` 清掉——这正是"不给老用户凭空塞一个 agent preset"的目地。代价是：如果有人在旧版里**实际用过**那个自动装上的预设却从没动过开关，升级后预设会消失（会话记录不受影响），重新打开开关即可恢复。
- `nextConfigFromBody` 只在 body **显式给出**字段时才改写，缺失即保留当前值——旧客户端只 POST 旧字段时，新字段不会被抹掉。
- `restoreDefaults: true` 返回整份 `HOST_DEFAULT_CONFIG`（含新字段）：恢复默认会清空角色文本并回到内置角色。
- 主题是配置的载体：角色开关 / 角色文本**按主题各存一份**，切换主题会连角色一起切。

## Host 半：preset 的生成与同步（`src/index.ts`）

常量（`src/index.ts:18-30`）：`DSH_HOME`、`AGENT_PRESET_ROOT`、`ROLEPLAY_PRESET_ID = 'xiao-roleplay'`、`ROLEPLAY_PRESET_DIR`、`ROLEPLAY_PRESET_NAME = '角色空间（娱乐）'`。

**路径解析（跨机器正确性的关键）**：预设根**不写死任何机器路径**，而是**逐条复刻** `@deepseek-ai/dsh-home-paths` 的 `resolveDshHome()`，顺序不能变：

1. `$DSH_HOME` 非空优先（空 / 全空白按未设置，避免解析到 cwd），否则 `homedir()/.dsh`；
2. 展开前导 `~` / `~/` / `~\`（只处理裸 `~` 与当前用户形式，`~user` 原样保留）——**漏了这步会让 `DSH_HOME=~/data` 的用户写到一个字面名为 `~` 的目录**；
3. `resolve()` 规范化成绝对路径（相对值按 cwd 解析）。

每台机器各自解析自己的家目录，Windows 上是 `C:/Users/<user>/.dsh/...`，macOS / Linux 上是 `/Users/<user>/.dsh/...` / `/home/<user>/.dsh/...`；设置页显示的绝对路径只是把分隔符统一成 `/` 后如实展示，可直接拿去文件管理器用。

**插件私有数据（设置 / 上传）的落点**：`CONFIG_PATH` 与 `UPLOAD_DIR` **不是**固定 `~/.dsh`，而是"跟随 DSH 数据根 + 老位置回落"，两个数据各自独立判定：

| 情况 | 选择 |
| --- | --- |
| `$DSH_HOME` 下的新位置**已有数据** | 新位置（用户明确把 DSH 数据放到别处就该跟着走；同一个根 = 可备份、可随盘迁移、不依赖家目录可写） |
| 新位置没有数据，但 `~/.dsh` 老位置**已有数据** | 老位置（老安装原地不动，升级不会看起来像"设置与上传全丢"） |
| 两者都没有（**新用户，含一开始就设了 `$DSH_HOME` 的人**） | 新位置 |

判定只发生在**进程启动时**（`existsSync`）一次：读与写必须是同一个目录，否则同一次运行里会出现"刚上传完列表却是空的"。`$DSH_HOME` 未设置时新旧位置是同一个路径，行为与历史版本逐字一致。老位置回落是为了兼容历史安装，**未来大版本可以在确认无老安装后删除**。

**老用户中途开始设 `DSH_HOME` 会怎样**（已实测）：设置与上传如果已经在 `~/.dsh`，就继续留在那里，**原样存活**；预设（必须和 DSH 一致）改写到 `$DSH_HOME/.agent-presets/`，因为 DSH 也改成扫描那里，功能依旧可用；旧位置的 `~/.dsh/.agent-presets/xiao-roleplay/` 变成孤儿（不报错、不生效），而一旦取消 `DSH_HOME`、DSH 又扫描 `~/.dsh`，插件启动时的同步会按当前角色文本把它写回一致内容——**自愈**，不会留下错角色的旧文件。真正会"看起来全新安装"的是 DSH 自己的数据（sessions / settings.yaml / credentials 都还在 `~/.dsh`），那是 DSH 层的行为，不是本插件。

| 函数 | 位置 | 职责 |
| --- | --- | --- |
| `ROLEPLAY_PERSONA_DEFAULT` | `index.ts:459` | 内置英文「魈」卡（唯一真实文本源；Client 不复制它，只用"空 = 默认"的语义） |
| `yamlLiteralBlock` | `index.ts:475` | 角色文本 → YAML 字面块标量 |
| `roleplayComposition` | `index.ts:491` | 生成 `agent.cordis.yml` |
| `roleplayMetadata` | `index.ts:511` | 生成 `preset.yml` |
| `writeIfChanged` | `index.ts:521` | 内容不同才写（避免无谓改 mtime 触发 preset 重新挂载） |
| `syncRoleplayPreset` | `index.ts:538` | 开 = 写 / 更新；关 = 删两个文件 + 非递归 `rmdir` |
| `syncRoleplaySafe` | `index.ts:554` | 只记日志的包装，绝不打断配置读写与设置页 |
| `roleplayInstalled` | `index.ts:564` | 状态查询（两个文件是否都在） |

同步触发点：插件启动（`index.ts:844`）、`POST /xiao-theme/settings`（`index.ts:873`）、主题新建 / 切换 / 删除（复用 `syncVoiceNow`，`index.ts:1213`）。

**YAML 转义规则（重要，别退回 `|-`）**：用**显式缩进指示符** `prefix: |2-` 配合固定 6 空格内容缩进。这样"首行非空行决定块缩进"的自动探测不生效，任意文本（冒号、引号、`#`、中文、空行、前导空格）都原样保留；只剥掉行尾空白，前导 tab 转空格（YAML 块缩进不接受 tab）。用 `|-` + 自动探测时，"首行带缩进 / 后续行不带缩进"的文本会解析错位甚至直接报错——这是踩过的坑。

生成的 `~/.dsh/.agent-presets/xiao-roleplay/agent.cordis.yml` 内容形态（供人工排查）：

```yaml
# Auto-generated by the xiao-ui-theme-ts plugin — do not hand-edit.
# ...
- id: persona
  name: '@deepseek-ai/dsh-persona'
  config:
    prefix: |2-
      <角色文本，逐行 6 空格缩进>
    complete: true
    includeRuntimeContext: false
```

`preset.yml`：`name: '角色空间（娱乐）'` + 中英双语 `description` + `order: 90`。

## 新增 HTTP 路由（全部 `kind: 'exact'`）

| 方法 | 路径 | 返回 |
| --- | --- | --- |
| GET / HEAD | `/xiao-theme/roleplay` | `{ presetId, presetName, enabled, installed, path }` |
| POST | `/xiao-theme/roleplay-apply` | 强制重写 → `{ ok, installed }`；失败 `400 { ok:false, error }` |
| POST | `/xiao-theme/roleplay-open-folder` | 用系统文件管理器打开预设目录 → `{ ok, path }`（关闭状态只建目录，不写预设） |

## Client 半：设置页结构（`src/client.ts`）

- 原「提示词」分组改名 **「语气（工作会话）」**（`voiceSection`）+ 边界说明（`voiceSectionHint`，`client.ts:1702`）。
- 新增独立分组 **「角色空间（娱乐）」**：`RoleplayGroup`（`client.ts:1513`），挂载点 `client.ts:1767`（位于「语气」与「吉祥物」之间）。内部：开关、角色 system prompt 文本域、应用 / 更新预设、恢复默认角色、打开预设文件夹、安装状态、两条说明 + 错误行。
- 文案键集中定义在 `STR`（`client.ts:56` 起）：`roleplaySection` 起共 14 个；API 封装在 `client.ts:418-435`。
- 组件接 `cfg` prop（父页面已订阅 store，不重复订阅）；文本域用 `key: persona`，保证「恢复默认角色」后重挂载显示新值。

## 维护者不得破坏的不变量

1. **工作会话永不注入角色身份**：`xiao-voice` section 只允许是语气文本；不要把 `roleplayPersona` 接进 `syncVoice`。
2. **角色预设不挂工具**：`roleplayComposition` 里不要为了"顺手"加 fs / bash / web 行。
3. **默认值方向**：`roleplayEnabled` 缺失 = **false**（默认关闭，对老用户友好：升级不会凭空多出 agent preset）。判定必须是"`=== true` 才开"，不要退回 "`!== false`"——那等于把缺失当开启，老配置会被重新装上预设。
4. **只删自己生成的文件**：关闭时只 `unlink` 两个已知文件名 + 非递归 `rmdir`，绝不做递归删除。
5. **不动用户目录里的其它内容**：预设目录只放 `agent.cordis.yml` / `preset.yml`。
6. **同步失败不上抛**：一律走 `syncRoleplaySafe`，角色功能失败不能让配置读写或设置页不可用。
7. **UI 不让用户编辑 composition 文本**：角色文本是**数据**（config 里的标量），由 Host 渲染进 preset；绝不让浏览器拼 YAML 或模块名（那等于让 UI 授予模块能力）。
8. **所有路径都要跟随 DSH 的 home 解析**：预设根、配置、上传一律不要写死 `~/.dsh`，也不要把 `DSH_HOME` 的优先级 / `~` 展开 / `resolve()` 三步顺序拆掉（漏了会让 `DSH_HOME=~/data` 的用户写错目录，表现为"设置说已安装，但新会话里选不到"）。
9. **插件数据的"老位置回落"要保留**：`CONFIG_PATH` / `UPLOAD_DIR` 的"新位置有数据用新位置，否则老位置有数据就用老位置"逻辑是给历史安装的兼容层；删除它会让老用户升级后看到默认设置与空上传列表。判定必须在启动时做一次，读与写不能各判各的。
10. **总开关必须门控角色预设**：生效条件是 `enabled !== false && roleplayEnabled === true`（Host 的 `syncRoleplayPreset`、状态路由、打开目录路由，以及 Client 的 `RoleplayGroup` 必须用同一判断）。不要为了"角色是独立功能"就让它绕过总开关——用户关掉「启用魈主题」而预设还活着，就是"看着关了其实没关"。UI 上：角色开关的 `checked` 显示用户自己的选择（`featureOn`），`disabled` 与其它控件一起受 `masterOn` 控制，状态行区分"总开关关闭"与"角色空间关闭"两种情况。

## 验证方法（本次已执行；验证脚本为一次性、已删除）

```bash
pnpm install
pnpm run typecheck     # tsc --noEmit
pnpm run build         # lib/index.js（Host）+ lib/client.js（ModuleLoader Client）
pnpm run check         # dsh 插件契约校验
```

集成验证用**伪 Host 上下文**在临时 HOME 下跑，两个关键点：

1. `ctx.inject(key, hook)` 要真的调用 `hook`；更关键的是 Cordis 的 `effect(fn)` 是"立即执行 + 登记清理"，**伪上下文必须真的执行 `fn`**，否则路由根本不会注册（踩过：报 `route missing`）。
2. `homedir()` 在模块加载时求值，所以要在**子进程启动前**设 `USERPROFILE`（Windows）/ `HOME`。
3. 上传路由会先 `await mkdir(...)` 再挂 `req` 的数据/结束监听，伪请求**必须在事件喂入前等一个 tick**，否则 `streamUpload` 永远收不到 end；而且 chunk 必须是 `Buffer`（代码直接调 `chunk.subarray`）。

**预设生成断言（已通过）**：`|2-` 块能逐字节回环自定义角色文本（前导 tab 按设计转空格）；`complete: true` + `includeRuntimeContext: false`；空文本回落内置英文「魈」；关闭开关后两个文件都被移除；再开启可重建。

**路由与兼容断言（已通过）**：默认 `roleplayEnabled = false` / `roleplayPersona = ''`，旧字段与 `xiao-voice` 注入不受影响；旧式局部写入（例如只传 `themeColor`）保留新字段；`restoreDefaults` 同时重置新旧字段；`/xiao-theme/roleplay` 在开 / 关两种状态下如实上报 `installed`。

**默认关闭断言（已通过，6 个场景）**：

| 场景 | 结果 |
| --- | --- |
| 全新安装（无配置文件） | `roleplayEnabled = false`，**不**安装预设，状态接口报 off |
| 旧配置（没有该字段，如只有 `themeColor`） | 同上，**不**安装预设 |
| 配置里显式 `true` | 安装预设，状态接口报 on |
| 配置里显式 `false` + 残留的预设文件（模拟旧版本自动装的） | 启动同步把残留**清掉** |
| 开关流程：off → POST true → 再 POST 只带 `themeColor` → POST false | 装 → 保持开启（不被旧式写入抹掉）→ 删 |
| `restoreDefaults: true` | 回到 off 并删掉预设 |

**总开关门控断言（已通过，4 个场景）**：

| 场景 | 结果 |
| --- | --- |
| `enabled: false` + `roleplayEnabled: true` + 已有预设文件 | 预设被**移除**，状态接口 `masterEnabled: false` / `enabled: false` |
| `enabled: true` + `roleplayEnabled: true` | 预设安装，状态 `masterEnabled: true` / `enabled: true` |
| `enabled: false` + 角色开关缺失 | 无预设，状态 inactive |
| 开关流程：两者都开 → POST `{enabled:false}` → POST `{enabled:true}` | 删预设（且 `roleplayEnabled` 仍是 `true`，用户选择不被抹掉）→ 重新装上 |

**路径解析断言（已通过）**：`DSH_HOME` 取绝对路径、`~/xxx` 与未设置三种形式，预设都落在 DSH 会扫描的根下且**不**写 `~/.dsh`；未设置 / 全空白时回落到 `~/.dsh`。另模拟"老用户中途设 `DSH_HOME`"：预置一份带自定义主色与角色文本的 `~/.dsh/xiao-theme.json` 后再设 `DSH_HOME`，`GET /settings` 仍返回原设置，且预设在新根下按原角色文本重新生成。（各用独立的 `USERPROFILE` / `DSH_HOME` 临时目录。）

**插件数据落点断言（已通过，共 4 个场景；用 `GET /settings` 读、写后查文件确认配置落点，用上传接口返回的 `imagePath` 反推上传目录）**：

| 场景 | 配置落点 | 上传落点 |
| --- | --- | --- |
| 新用户 + 一开始就设 `$DSH_HOME`（两边都空） | `$DSH_HOME` | `$DSH_HOME` |
| 老用户（设置 + 上传目录都在 `~/.dsh`）+ 中途设 `$DSH_HOME` | `~/.dsh` | `~/.dsh` |
| 未设 `$DSH_HOME`（历史默认行为） | `~/.dsh` | `~/.dsh` |
| 老用户只有设置、从未上传过 + 设 `$DSH_HOME` | `~/.dsh` | `$DSH_HOME`（两个数据各自独立判定） |

（第 4 行是刻意的：只有真正没有数据的那个数据才搬到新根。）

## 已知限制与后续可做

1. **没有「一键开始角色会话」**：DSH 目前未向第三方客户端插件暴露"暂存新会话预设"的接口（`dsh-client-ui-agent-preset` 的 seat controller 不提供 service，`ISessions.create` 也不接受 preset）。因此入口是"自动安装 + 状态展示 + 打开目录 + 明确指引"。若后续 DSH 暴露该能力，可直接在 `RoleplayGroup` 加按钮。
2. **preset 只能在空会话切换**（DSH 规则）：进入角色必须新开会话，已有会话无法改成角色会话——这是设计而非缺陷。
3. **零工具 preset 尚未在真实 DSH 会话里实跑**：本次只验证了文件生成与路由，没有真起一个角色会话。若上游对空工具目录有约束，退路是加一个无害的只读 / 提问工具。
4. **角色文本随主题走**：与现有主题模型一致；若希望角色独立于主题，需要把这两个字段移出 `XiaoConfig`、落到独立文件。
5. **只看得到 `$DSH_HOME`，看不到 composition 级显式 home**：DSH 的优先级是"显式配置 > `$DSH_HOME` > `~/.dsh`"，插件没有渠道读取宿主 composition 里的显式 home。若部署方用显式配置把 home 挪走，预设会落到 `$DSH_HOME` / `~/.dsh`，同样出现"选不到预设"。将来若 DSH 暴露 home 服务，应改为消费它而不是读环境变量。
6. **本次未改 `package.json` 版本号**，发布节奏由维护者决定。

## 文件索引

| 文件 | 本次改动 |
| --- | --- |
| `src/config.ts` | `XiaoConfig` 新增 2 个字段 |
| `src/index.ts` | preset 生成 / 同步、3 条 HTTP 路由、启动与配置变更挂钩 |
| `src/client.ts` | 语气分组改名 + 边界说明；`RoleplayGroup`；14 个文案键；3 个 API 封装 |
| `README.md` / `README.zh-CN.md` / `README.ja.md` / `README.ko.md` | 功能、设置页、使用与配置、注意事项四节同步更新 |
